# Backend Environment Variables
# Copy this file to .env and update with your actual values
# Command: cp .env.example .env  (Linux/Mac)
# Command: copy .env.example .env  (Windows)

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB: mongodb://localhost:27017/your-database-name
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database-name
MONGODB_URI=mongodb://localhost:27017/mern-boilerplate
MONGO_URI=mongodb://localhost:27017/mern-boilerplate

# JWT Configuration
# IMPORTANT: Change these to secure random strings in production (minimum 32 characters)
# You can generate secure secrets using: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-characters
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-change-this-in-production-min-32-characters

# JWT Token Expiration
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Frontend URL (for CORS)
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000
